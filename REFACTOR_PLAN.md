# NovaOps 重构方案：企业级 RAG 知识库与 Agent 智能问答平台

> 本文档由 `refactor.md`（项目重构原始笔记）优化整理而来，原文保留不动。
> 整理日期：2026-08-23
> 现状基线：前端 Vue 3.5 + TS + Ant Design Vue 4 + Pinia + MSW；后端 Spring Boot 3.4.5 + MyBatis + MySQL 8（auth / ticket 已接入真实后端，kb 页面目前为前端占位）

---

## 1. 重构目标与范围

将现有企业管理后台（登录鉴权、权限、工单、资产、看板）重构为一个**面向企业的后台管理平台，新增 RAG 知识库与 Agent 智能问答能力**。

重构范围分为两大部分：

1. **新能力**：知识库管理（文档上传 → 解析 → 向量化 → 检索）+ Agent 智能问答（基于知识库的 RAG 问答，带防幻觉校验，流式输出）。
2. **存量优化**：登录页改版、多用户单身份的权限架构、组件库使用规范化、全局主题色与深浅色切换。

不在本次范围内：工单 / 资产 / 看板模块的业务逻辑重构（仅做必要的适配）。

---

## 2. 总体架构

### 2.1 架构总览

```
┌─────────────────────────── 前端 SPA (Vue 3 + AntD Vue) ───────────────────────────┐
│  登录页(改版)   布局/主题   知识库管理页   Agent对话页   客服浮窗(全局)   用户管理页   │
│       │              │            │             │            │            │        │
│       └──────────────┴──── Axios / SSE 工具 ────┴────────────┴────────────┘        │
└──────────────────────────────────────┬─────────────────────────────────────────────┘
                                       │ HTTPS (JWT)
┌──────────────────────────────────────▼─────────────────────────────────────────────┐
│                     后端 Spring Boot 3 + Spring AI                                  │
│  ┌──────────┐  ┌──────────────┐  ┌────────────────┐  ┌─────────────────────────┐   │
│  │ auth     │  │ ticket(已有) │  │ kb 模块        │  │ agent 模块              │   │
│  │ 用户/身份 │  │              │  │ 上传/解析/向量化 │  │ RAG问答/校验/SSE流式     │   │
│  └──────────┘  └──────────────┘  └───────┬────────┘  └───────────┬─────────────┘   │
│       common: JWT 鉴权拦截 / 统一响应 / 全局异常 / 工具封装                          │
└──────────────────────────────────┬──────┴──────────────┬───────────────────────────┘
                        ┌──────────▼─────────┐  ┌────────▼──────────────────┐
                        │  MySQL 8           │  │  Qdrant 向量库             │
                        │  业务数据/文档元数据 │  │  chunk 向量 + payload 过滤 │
                        └────────────────────┘  └────────┬──────────────────┘
                                                         │ Embedding / Chat
                                                 ┌───────▼─────────────────────┐
                                                 │  SiliconFlow API (OpenAI 兼容)│
                                                 │  API-KEY 仅存于后端环境变量    │
                                                 └─────────────────────────────┘
```

### 2.2 模块边界与职责（避免 RAG 与 Agent 相互耦合冲突）

| 模块 | 职责 | 明确不做 |
| --- | --- | --- |
| `kb`（知识库） | 文档上传/解析/分块/向量化入库、文档 CRUD、**对内提供检索服务** `retrieve(query, topK, minScore)` | 不关心问答与提示词，不调用 Chat 模型 |
| `agent`（问答） | 组装 RAG 链路：调用 kb 检索服务 → 构造约束 Prompt → 调用 LLM → 输出校验 → SSE 流式返回 | 不直接操作向量库、不负责文档管理 |
| `auth` | 用户、身份（角色）、菜单权限 | — |

**协作契约**：agent 只通过 kb 暴露的内部 Service 接口（或 REST）获取检索结果，二者共用同一份 chunk 元数据结构（含 `documentId`、来源片段），保证引用可回溯。知识库为单租户统一存储，检索时不再需要 tenantId 过滤（单租户化改造后已移除）。

---

## 3. 技术选型与决策

| 领域 | 选型 | 理由 | 备选与否决原因 |
| --- | --- | --- | --- |
| Agent 框架 | **Spring AI（1.0.x GA+）** `spring-ai-starter-model-openai` | 与 Spring Boot 3.4 生态原生集成；SiliconFlow 兼容 OpenAI 协议，只需改 `base-url`；自带 ChatClient/EmbeddingModel/VectorStore 抽象和流式 API | LangChain4j（重复造轮子，Spring 侧集成弱）；自研 HTTP 调用（无流式/重试/抽象，维护成本高） |
| LLM / Embedding | **SiliconFlow API**（OpenAI 兼容端点） | 原始需求指定。Chat 与 Embedding 模型名均做成配置项（建议 Embedding 默认 `BAAI/bge-m3`，中文效果好、1024 维） | — |
| 向量数据库 | **Qdrant**（Docker 单容器部署） | ① Spring AI 官方提供 `spring-ai-qdrant-store` 集成；② 单二进制/单容器，运维极轻，符合中后台项目体量；③ payload 过滤天然支持 `documentId` 隔离与级联删除；④ 与 MySQL 职责不重叠 | pgvector（需引入 PostgreSQL，出现第二套关系型库，职责重复）；Milvus（依赖 etcd+MinIO，组件重，适合大规模场景）；Redis（向量能力绑定 Redis Stack，生态受限） |
| 文档解析 | **Apache Tika** 统一解析 md / pdf / doc / docx | 一个入口覆盖全部目标格式，屏蔽格式差异；doc 老格式由 Tika 内部调用 POI 处理 | 每种格式引一个库（PDFBox + POI 手工组合，解析状态机要自己维护） |
| 流式输出 | 后端 SSE（Spring MVC + `SseEmitter` / `Flux<ServerSentEvent>`）；前端 `fetch` + `ReadableStream` 封装 SSE 解析 | SSE 单向流足够（对话场景无需 WebSocket）；`EventSource` 不支持 POST 和自定义 Authorization 头，故用 fetch 封装 | WebSocket（双向能力用不上，握手/心跳/断连状态机复杂度更高） |
| 前端框架/组件 | 维持 Vue 3 + TS + **Ant Design Vue 4** + Pinia | 现有栈不变，AntD Vue 4 的 ConfigProvider 原生支持主题 token 与深浅色算法 | — |

> 版本注意：引入 Spring AI 时需验证与其 BOM 和 Spring Boot 3.4.5 的兼容矩阵，以后端实际启动编译通过为准。

---

## 4. 知识库模块设计

### 4.1 数据模型（MySQL 存元数据，Qdrant 存向量）

```sql
kb_document (
  id, title, file_name, file_type,                    -- md/pdf/doc/docx
  file_size, status,                                  -- PARSING / VECTORIZING / READY / FAILED
  chunk_count, error_msg, created_by, created_at, updated_at, deleted
)
kb_chunk (
  id, document_id, chunk_index,
  content,                                            -- 分块原文（用于引用回显）
  vector_id                                           -- 指向 Qdrant 点位，删除时级联清理
)
```

- Qdrant 中每个 point 的 payload 携带 `documentId`、`chunkId`：检索范围为全量知识库，删文档时按 `documentId` 批量删除向量。
- 解析采用**异步流水线**：上传接口立即返回 `PARSING` 状态，解析+向量化在后台执行，前端轮询或 SSE 通知最终状态（`READY` / `FAILED` + 原因），失败可重新上传。

### 4.2 文档处理流水线

```
上传(multipart) → 校验(类型白名单/大小上限) → 落盘/对象存储 → Tika 抽取纯文本
→ 清洗(去页眉页脚/乱码) → 分块(按标题+段落，约500~800 token，重叠50~100，参数可配)
→ 批量 Embedding(SiliconFlow) → 写入 Qdrant → 更新 MySQL 状态为 READY
```

### 4.3 检索服务（kb 模块对内暴露）

```java
interface KbRetrievalService {
    RetrievalResult retrieve(String query, int topK, double minScore);
    // 返回命中的 chunk 列表（含原文、来源文档名、相似度得分），供 agent 组装 Prompt 与引用回溯
}
```

低于 `minScore` 阈值的结果直接丢弃；命中为空时 agent 走拒答兜底（见 5.2）。

### 4.4 管理端页面（仅管理员可见）

- 文档列表：`a-table` 分页 + 按文件名/类型/状态筛选 + 状态 Tag（解析中/就绪/失败）。
- 上传：`a-upload` 拖拽上传，前端预校验类型（md/pdf/doc/docx）与大小上限，展示解析进度。
- 删：删除需二次确认，后端**先删向量再删元数据**，保证不一致时可重试。
- 改：支持编辑文档标题/重新上传替换（旧向量先清后建）。
- 查：文档详情抽屉展示分块预览，便于排查"为什么检索不到"。

---

## 5. Agent 智能问答设计

### 5.1 RAG 问答链路

```
用户提问 → agent 模块 → kb 检索(topK, minScore)
→ 命中为空？→ 是：返回固定兜底话术（"知识库中暂无相关内容…"），不调用 LLM
→ 否：组装 Prompt（系统约束 + 检索片段带编号 [1][2]… + 用户问题）
→ SiliconFlow Chat 流式生成 → 输出校验（见 5.2）→ SSE 分片推给前端
```

### 5.2 防幻觉三道防线（输出核对模块）

原始需求要求"设计一个核对 LLM 输出是否确切经过知识库检索的模块"，落地为检索、生成、校验三层的组合：

1. **检索侧（源头约束）**：相似度阈值 `minScore` + topK 截断；无可靠依据时直接拒答，不给模型"编"的机会。
2. **生成侧（Prompt 约束）**：系统提示词强制要求"仅依据提供的资料回答、逐条标注引用编号 [n]、资料未覆盖时明确说明不知道"。
3. **校验侧（输出核对）**：回答完成后运行**引用回溯校验器**——
   - 提取回答中的引用编号，校验其是否真实存在于本次检索结果集合（防伪造引用）；
   - 对回答句子与所引 chunk 做 embedding 相似度复核（可选增强：轻量 LLM 作 Judge 复核论断-依据一致性）；
   - 校验不通过 → 触发一次重新生成；仍不通过 → 在回答上附加"⚠ 该回答未通过知识库依据校验，仅供参考"标识后返回。

### 5.3 API 设计（SSE 流式）

```
POST /api/agent/chat        (SSE)  请求体: { conversationId?, content }
                                   响应流事件: delta(增量文本) / citation(引用列表)
                                              / meta(校验结果/耗时) / done / error
GET  /api/agent/conversations        会话列表
GET  /api/agent/conversations/{id}   历史消息
```

健壮性约定：SSE 连接断开时后端及时终止上游 LLM 调用（避免浪费 token）；LLM 调用设置超时与有限次重试；所有事件带 `conversationId` 便于前端乱序兜底。

### 5.4 前端两个入口，共用一套实现

| 入口 | 形态 |
| --- | --- |
| 全局客服浮窗 | 页面右下角固定"客服"按钮（挂载于主布局），点击弹出可拖拽/可关闭的对话弹窗（`a-float-button` + Drawer/Modal） |
| 独立对话页 | 用户信息栏（顶栏头像下拉）快捷入口 → 跳转 `/agent/chat` 全功能页（含会话列表、历史记录、引用来源面板） |

两个入口**复用同一个聊天核心组件 + 同一个 `useChat` composable + 同一个 Pinia 会话 store**，差异只在容器与布局；流式渲染（打字机效果、markdown 渲染）在核心组件内统一实现。

---

## 6. 安全设计（密钥安全）

1. **API-KEY 仅存于后端环境变量**：`SILICONFLOW_API_KEY`，Spring 配置以 `${SILICONFLOW_API_KEY:}` 占位引用；`.env*` 与 `application-local.yml` 均已在 `.gitignore` 中，密钥不入库。
2. **前端永不接触密钥**：所有 LLM / Embedding 调用一律由后端代理转发，前端只与自家后端通信。
3. **传输加密**：部署侧启用 HTTPS（TLS），密钥与对话内容在传输层加密；本地开发可用 HTTP，生产必须 TLS。
4. **日志脱敏**：日志中禁止输出 API-KEY、Authorization 头、完整 Prompt（只记长度/哈希）；统一在日志工具中做脱敏封装。
5. **权限收口**：`/api/kb/**` 管理接口仅管理员角色可访问；`/api/agent/**` 登录即可；沿用现有 JWT 鉴权拦截器，SSE 请求同样校验 token。

---

## 7. 存量问题改造

### 7.1 登录页改版

- 布局调整为：**左侧约 2/3 为品牌背景图**（叠加渐变遮罩 + 产品标语），**右侧约 1/3 为登录表单卡片**；窄屏降级为单列（移动端兜底）。
- 表单逻辑（账号 + 身份选择 + 自动注册）保持不变。

### 7.2 多用户单身份架构 + 身份目录

- 架构约束：一个用户**有且仅有一个身份**（用户表与角色一对一，登录时的身份选择即该账号的固定身份）。
- 新增**身份目录**（角色字典：管理员 / 运维人员 / 访客，含各身份的权限码集合说明），由后端 `/api/auth/roles` 扩展支撑。
- 新增**用户管理页（仅系统管理员可见）**：用户列表、搜索、启用/禁用、重置密码、调整身份（单选，切换后即时生效）。
- 权限约束贯穿三层：**路由级**（动态路由按身份过滤）、**页面级**（菜单可见性）、**按钮级**（现有权限指令/组件控制操作按钮显隐）。非管理员用户不能看到用户管理、知识库管理等入口。

### 7.3 组件库使用规范

- 优先使用 Ant Design Vue 成熟组件（Upload、Table、Modal、Drawer、FloatButton、Segmented、Statistic 等），**不重复造轮子**；自研封装仅限业务层（如通用查询表格 Hook 在 AntD Table 之上做业务配置化封装），并在封装处体现对组件库的定制配置能力（透传 props、定制 column/render、主题 token）。
- 现有自研通用组件保留，但内部如与组件库能力重复则逐步收敛替换。

### 7.4 全局主题切换

- 基于 `ConfigProvider` 的 `theme.token`（`colorPrimary` 等）实现**全局主题色切换**（预置若干主题色 + 自定义色板）。
- 基于 `theme.algorithm`（`darkAlgorithm` / `defaultAlgorithm`）实现**深浅色切换**，并支持"跟随系统"选项。
- 主题状态由 Pinia store 管理 + `localStorage` 持久化；ECharts 图表与自定义样式同步读取 CSS 变量/主题状态，避免深色下出现刺眼白块。

---

## 8. 分阶段执行计划

每个阶段完成后**先验收再进入下一阶段**，验收标准如下。阶段过程与成果记录于 `docs/refactor-journal/phase-{N}.md`（含：完成了什么、验收结果、为什么这样设计）。

| 阶段 | 内容 | 交付物 | 验收标准 |
| --- | --- | --- | --- |
| **P0 存量 UI 改造** | 登录页 2/3+1/3 改版；主题色切换 + 深浅色切换；组件库使用收敛 | 改版登录页、theme store、文档说明 | 登录页布局符合设计且窄屏可用；切换主题色/深浅色全站生效且刷新后保持；无白块/样式残留 |
| **P1 身份与权限收敛** | 身份目录接口与页面；用户管理页（仅管理员）；单身份约束落到后端 | 用户管理页、roles 扩展接口、后端校验 | 非管理员无法访问用户管理（路由+接口双重拦截）；身份调整即时生效；一个用户仅一个身份 |
| **P2 知识库端到端** | Qdrant 部署、文档上传/解析/分块/向量化流水线、文档 CRUD 接口与管理页 | kb 后端模块、kb 管理页改造 | 上传 md/pdf/doc 各一例均能解析并向量化（状态 READY）；删文档后向量同步清除；检索覆盖全量知识库；失败状态可读、可重传 |
| **P3 Agent 问答端到端** | Spring AI + SiliconFlow 接入、RAG 链路、防幻觉三道防线、SSE 流式接口、客服浮窗 + 独立对话页 | agent 后端模块、两个前端入口 | 提问命中知识库时流式输出正常（浮窗与页面一致）；无依据时拒答；伪造引用/离题回答能被校验器识别并标注；密钥全程不出现在前端与日志 |
| **P4 加固与收尾** | 边界与兜底逻辑补全、双模式（MSW/真实后端）对齐、整体回归 | 兜底逻辑清单落地、回归记录、**项目总结文档**（选型理由、前后端架构、RAG 链路、从零理解项目的导览） | 异常场景（向量库宕机/LLM 超时/断网/大文件/非法类型）均有兜底提示而非白屏报错；总结文档可支撑从零理解本项目 |

**阶段记录模板**（`docs/refactor-journal/phase-{N}.md`）：

```md
# Phase N：xxx
## 本阶段完成任务
## 验收结果（逐条对照验收标准）
## 设计思路与取舍
## 遗留问题
```

---

## 9. 工程规范

### 9.1 后端

- 统一响应 `ApiResponse` / 分页 `PageResult` / 全局异常处理（复用现有 common 包），所有新接口遵循同一契约；入参一律 JSR-303 校验。
- 模块间**低耦合**：auth / ticket / kb / agent 相互只通过 Service 接口或事件通信，禁止跨模块直连对方 Mapper；可复用逻辑（文件校验、分块、SSE 工具、日志脱敏）下沉到 common/util。
- LLM/Embedding 外呼统一封装：超时、有限重试、异常降级一处管理。

### 9.2 前端

- 遵循 **SPA + 组件化 + MVVM**：视图不写业务逻辑，状态进 Pinia store（View → store → api 分层）；可复用交互逻辑抽 composable（`useChat`、`useTheme`、上传轮询等）。
- API 层统一在 `src/api` 定义，与后端接口一一对应并附类型；SSE 请求封装为独立工具（fetch + ReadableStream，token 注入、断线重连、超时处理）。

### 9.3 兜底逻辑清单（P4 逐项核对）

| 场景 | 兜底 |
| --- | --- |
| SSE 中途断开 | 前端提示并支持重连/重发；后端终止上游调用 |
| LLM 超时/限流 | 后端有限重试 → 降级为友好错误事件；前端展示可重试按钮 |
| 向量库不可用 | 健康检查提示 + 问答走"知识库暂不可用"兜底话术，不影响其他模块 |
| 上传非法类型/超大文件 | 前端预校验 + 后端白名单与大小硬校验，返回明确错误 |
| 解析/向量化失败 | 状态回显失败原因，支持重新上传，不阻塞其他文档 |
| 检索无结果 | 固定拒答话术，不调用 LLM |
| token 过期 | 沿用现有刷新 token / 401 跳登录机制，SSE 请求同样处理 |

---

## 10. 风险与应对

| 风险 | 应对 |
| --- | --- |
| Spring AI 版本与 Boot 3.4.5 兼容问题 | 引入时先做最小可行集成（一次 Chat 调用）验证，锁定 BOM 版本 |
| doc（老二进制格式）解析质量差 | Tika + 失败状态回显 + 允许用户转存 docx 重传；文档说明格式建议 |
| Qdrant 需要额外部署 | Docker 单容器 + `docker-compose` 一键脚本，纳入 STARTUP_GUIDE |
| SiliconFlow 限流/网络抖动 | 超时+重试+熔断降级；批量 Embedding 做分批与退避 |
| 长文档分块不当导致检索效果差 | 分块大小/重叠可配置；kb 详情页提供分块预览辅助排查 |
| MSW 与真实后端双模式漂移 | 新增接口同步补 Mock，保持两种运行方式均可演示 |

---

## 11. 交付物清单

- 后端：`kb` 模块（上传/解析/向量化/检索/CRUD）、`agent` 模块（RAG 问答/校验/SSE）、用户与身份管理接口扩展。
- 前端：改版登录页、主题切换、用户管理页、知识库管理页、Agent 对话页 + 全局客服浮窗。
- 数据与部署：MySQL 增量 DDL、Qdrant docker-compose、`.env.example`（密钥占位说明）。
- 文档：`docs/refactor-journal/phase-{N}.md` 阶段记录 × 5、项目总结文档（选型理由、架构说明、前后端模块导览）、STARTUP_GUIDE 更新。
