<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novaops.backend.agent.mapper.AgentMapper">
  <insert id="insertConversation">insert into agent_conversation(id,tenant_id,user_id,title,created_at,updated_at) values(#{id},#{tenantId},#{userId},#{title},now(),now())</insert>
  <select id="findConversation" resultType="com.novaops.backend.agent.model.ConversationRecord">select * from agent_conversation where tenant_id=#{tenantId} and user_id=#{userId} and id=#{id} limit 1</select>
  <select id="listConversations" resultType="com.novaops.backend.agent.model.ConversationRecord">select * from agent_conversation where tenant_id=#{tenantId} and user_id=#{userId} order by updated_at desc limit 100</select>
  <update id="touchConversation">update agent_conversation set updated_at=now() where id=#{id}</update>
  <insert id="insertMessage">insert into agent_message(id,conversation_id,role,content,citations_json,validation_passed,created_at) values(#{id},#{conversationId},#{role},#{content},#{citationsJson},#{validationPassed},now())</insert>
  <select id="listMessages" resultType="com.novaops.backend.agent.model.AgentMessageRecord">select * from agent_message where conversation_id=#{conversationId} order by created_at,id</select>
</mapper>
