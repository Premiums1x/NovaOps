server:
  port: 8090

spring:
  application:
    name: novaops-backend
  datasource:
    url: ${NOVAOPS_DB_URL:jdbc:mysql://127.0.0.1:3306/novaops?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8}
    username: ${NOVAOPS_DB_USERNAME:root}
    password: ${NOVAOPS_DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      max-file-size: ${NOVAOPS_KB_MAX_FILE_SIZE:30MB}
      max-request-size: ${NOVAOPS_KB_MAX_FILE_SIZE:30MB}
  ai:
    retry:
      max-attempts: ${SILICONFLOW_MAX_ATTEMPTS:2}
      backoff:
        initial-interval: 500ms
        max-interval: 2s
        multiplier: 2
    openai:
      base-url: ${SILICONFLOW_BASE_URL:https://api.siliconflow.cn}
      api-key: ${SILICONFLOW_API_KEY:local-not-configured}
      chat:
        options:
          model: ${SILICONFLOW_CHAT_MODEL:Qwen/Qwen3-8B}
      embedding:
        options:
          model: ${SILICONFLOW_EMBEDDING_MODEL:BAAI/bge-m3}
  mail:
    host: ${SMTP_HOST:smtp.qq.com}
    port: ${SMTP_PORT:465}
    username: ${SMTP_USERNAME:}
    password: ${SMTP_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

app:
  security:
    # 不提供默认值：缺失时启动即失败，避免用公开在仓库里的默认密钥签发可伪造的 token
    jwt-secret: ${NOVAOPS_JWT_SECRET:}
    access-token-expire-seconds: 1800
    refresh-token-expire-days: 7
  mail:
    mode: ${MAIL_MODE:log}
    from: ${MAIL_FROM:}
    # 激活/重置链接里的前端地址前缀，log 模式下也会直接返回给前端
    activation-base-url: ${MAIL_ACTIVATION_BASE_URL:http://localhost:5173}
  kb:
    storage-path: ${NOVAOPS_KB_STORAGE:./data/kb}
    max-file-size-bytes: ${NOVAOPS_KB_MAX_FILE_BYTES:31457280}
    chunk-tokens: ${NOVAOPS_KB_CHUNK_TOKENS:600}
    default-top-k: ${NOVAOPS_KB_TOP_K:5}
    default-min-score: ${NOVAOPS_KB_MIN_SCORE:0.55}
    qdrant-base-url: ${QDRANT_BASE_URL:http://localhost:6333}
    qdrant-collection: ${QDRANT_COLLECTION:novaops_kb}
    qdrant-api-key: ${QDRANT_API_KEY:}
  agent:
    plan-enabled: ${NOVAOPS_AGENT_PLAN_ENABLED:true}
    sse-timeout-ms: ${NOVAOPS_AGENT_SSE_TIMEOUT_MS:120000}
    top-k: ${NOVAOPS_AGENT_TOP_K:5}
    min-score: ${NOVAOPS_AGENT_MIN_SCORE:0.55}
    retrieval-validation-min-score: ${NOVAOPS_AGENT_RETRIEVAL_VALIDATION_MIN_SCORE:0.5}
    metadata-max-documents: ${NOVAOPS_AGENT_METADATA_MAX_DOCUMENTS:200}
    executor-core-size: ${NOVAOPS_AGENT_EXECUTOR_CORE_SIZE:4}
    executor-max-size: ${NOVAOPS_AGENT_EXECUTOR_MAX_SIZE:12}
    executor-queue-capacity: ${NOVAOPS_AGENT_EXECUTOR_QUEUE_CAPACITY:100}
    tool-executor-core-size: ${NOVAOPS_AGENT_TOOL_EXECUTOR_CORE_SIZE:8}
    tool-executor-max-size: ${NOVAOPS_AGENT_TOOL_EXECUTOR_MAX_SIZE:16}
    tool-executor-queue-capacity: ${NOVAOPS_AGENT_TOOL_EXECUTOR_QUEUE_CAPACITY:0}
    task:
      max-steps: ${NOVAOPS_AGENT_TASK_MAX_STEPS:10}
      max-revisions: ${NOVAOPS_AGENT_TASK_MAX_REVISIONS:2}
      observation-max-chars: ${NOVAOPS_AGENT_TASK_OBSERVATION_MAX_CHARS:2000}
      step-timeout-seconds: ${NOVAOPS_AGENT_TASK_STEP_TIMEOUT_SECONDS:20}
      stale-minutes: ${NOVAOPS_AGENT_TASK_STALE_MINUTES:30}
    mcp:
      server:
        token: ${NOVAOPS_AGENT_MCP_SERVER_TOKEN:}
      remote:
        servers: []
      sse-timeout-ms: ${NOVAOPS_AGENT_TASK_SSE_TIMEOUT_MS:0}
